# Stacks legadas — Docker Swarm + Traefik

Estes arquivos são a configuração original de produção (Swarm, rede overlay, labels Traefik).

**Não são usados** na instalação atual. A stack ativa é o `docker-compose.yml` na raiz do projeto.

Mantidos apenas como referência histórica.
